import { writable } from 'svelte/store';

export const collaboratorsList = writable([]);
export const cardsListInit = writable([]);
export const cardsList = writable([]);
export const cardDetails = writable({});
